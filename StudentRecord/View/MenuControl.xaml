<UserControl x:Class="StudentRecord.View.MenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Menu>
        <MenuItem Header="File">
            <MenuItem Header="New" Command="{Binding NewRecordCommnad}" />
            <MenuItem Header="Address" Command="{Binding AddressCommand}" />
            <MenuItem Header="Exit" Command="{Binding ExitCommnad}" />
        </MenuItem>
        <MenuItem Header="Records">
            <MenuItem Header="Address" Command="{Binding ShowAddressViewCommand}" />
            <MenuItem Header="Student" Command="{Binding ShowStudentViewCommand}" />
        </MenuItem>
    </Menu>
</UserControl>
